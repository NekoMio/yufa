use grammar_struct_lib::grammar_struct::*;

/// LR1 语法分析器
///
/// ```
/// let result_lr = run_lr1(str, grammar);
/// ```
/// 返回运行循环次数，或者错误
pub fn run_lr1(contents: &str, grammar: &Grammar) -> Result<u32, String> {
    return Ok(0);
}
